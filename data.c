#include <stdio.h>
int main()
{
    unsigned int count1;
    signed int  count2;
    unsigned char count3;
    signed char count4;
    printf(" %u bytes", sizeof(unsigned int));
    printf(" %u bytes", sizeof(signed int));
    printf(" %u bytes", sizeof(unsigned char));
    printf("%u bytes", sizeof(signed char));


return 0;

}
